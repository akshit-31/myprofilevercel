# myprofile

This is my profile website built with Flask.

## Deployment to Vercel

1. Push this code to a GitHub repository.
2. Connect the repository to Vercel.
3. Vercel will automatically detect the Python app and deploy it using the vercel.json configuration.

The app is configured for serverless deployment with static files served correctly.
